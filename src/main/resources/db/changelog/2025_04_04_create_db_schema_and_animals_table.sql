--liquibase formatted sql

--changeset Tsvetan.Todorov:1
-- create table user_svc.animals
-- (
--     name  varchar(50) not null,
--     age   int         not null,
--     color varchar(50) not null
-- );

--rollback truncate document